public class Main {
    /* 2 pointers is all about converting 2,3 loops into o(n) Time. */

    /*
    Q. When 2 Pointers Question comes
    A. a. When given DSA is array/string only generally.
       b. When given Array/LinkedList gives as sorted/ ask to sort.
       c. merge/ remove / re-arrange / duplicate.
       d. pair / triplet / quadret.
       e. detect-cycle in Linkedlist.
     */
    public static void main(String[] args) {

        /*
        1. Finding Pair without using extra space(Hashmap etc)
           --> its 2 pointer.
         */
    }
}